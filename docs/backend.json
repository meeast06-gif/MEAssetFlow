{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public-facing profile information.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's chosen display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        }
      }
    },
    "Asset": {
      "title": "Asset",
      "type": "object",
      "description": "Represents a single financial or tangible asset owned by a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns this asset."
        },
        "name": {
          "type": "string",
          "description": "The common name or description of the asset (e.g., 'Apple Stock')."
        },
        "type": {
          "type": "string",
          "enum": [
            "Stocks",
            "Bonds",
            "Real Estate",
            "Savings",
            "Crypto",
            "Other"
          ],
          "description": "The category of the asset."
        },
        "currentValue": {
          "type": "number",
          "description": "The current estimated monetary value of the asset."
        },
        "acquisitionDate": {
          "type": "string",
          "format": "date-time",
          "description": "The date when the asset was acquired."
        },
        "notes": {
          "type": "string",
          "description": "Optional additional notes about the asset."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the record was created."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the record was last updated."
        }
      },
      "required": [
        "userId",
        "name",
        "type",
        "currentValue",
        "acquisitionDate",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profile information for each user."
    },
    "/users/{userId}/assets/{assetId}": {
      "schema": {
        "$ref": "#/entities/Asset"
      },
      "description": "Stores the collection of assets owned by a specific user."
    }
  }
}
